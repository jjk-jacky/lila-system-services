#!@BINDIR@/execlineb -P
aa-service
s6-envdir /etc/mount/${INSTANCE}
importas -ui fstype fstype
importas -uD rw options options
importas -ui device device
importas -ui mountpoint mountpoint
aa-mount -dt ${fstype} -o ${options} ${device} ${mountpoint}
