#!@BINDIR@/execlineb -P
aa-service
if { aa-test -r /etc/network/shared-interface }
backtick -n INTERFACE { cat /etc/network/shared-interface }
importas -iu INTERFACE INTERFACE
if { iptables -D FORWARD -i ${INSTANCE} -o ${INTERFACE} -j ACCEPT }
