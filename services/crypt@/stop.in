#!@BINDIR@/execlineb -P
aa-service
importas -u -D luks type type
backtick -n name { ifte
  { cat /etc/crypt/${INSTANCE}/name }
  { echo ${type}-${INSTANCE} }
  aa-test -r /etc/crypt/${INSTANCE}/name
}
importas -u name name
cryptsetup close ${name}
